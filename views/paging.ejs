<br />
<div>
  <% if (!isFirstPage) { %>
    <span class="prepage"><a title="Page Up" href="?p=<%= page-1 %>">Page Up</a></span>
  <% } %>

  <% if (!isLastPage) { %>
    <span class="nextpage"><a title="Page Down" href="?p=<%= page+1 %>">Page Down</a></span>
  <% } %>
</div>